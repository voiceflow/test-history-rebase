# Setup Instructions
Run npm install in main directory and app directory. React front-end code is in app dir.

-npm run redis   
-sudo npm run server   
-npm run client  

Just for release

Firefox complains about mixed content due to http websocket so disable it [](https://www.thewindowsclub.com/disable-mixed-content-firefox)
