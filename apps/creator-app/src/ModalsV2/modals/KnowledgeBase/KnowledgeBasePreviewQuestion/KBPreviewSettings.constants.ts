// this is modal height when instructions and system textareas are empty
// if we change the height of the modal, we need to change this value
export const MODAL_DEFAULT_HEIGHT = 467;

// if we change line height or padding of textarea, we need to change this value
export const TEXT_AREA_NEW_LINE_HEIGHT = 20;

// if we change modal padding bottom or top, we need to change this value
export const MODAL_PADDING = 32;

// if we remove or add textareas, we need to change this value
export const NUMBER_OF_TEXT_AREAS = 2;
