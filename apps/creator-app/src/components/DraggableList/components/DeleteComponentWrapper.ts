import { styled } from '@/hocs/styled';

const DeleteComponentWrapper = styled.div`
  position: sticky;
  bottom: 0;
  width: 100%;
  height: 110px;
  pointer-events: none;
  background: #fdfdfd;
  border-top: 1px solid #eaeff4;
  border-bottom: 1px solid #eaeff4;
`;

export default DeleteComponentWrapper;
