export enum PluginType {
  LINKS = 'LINKS',
  VARIABLES = 'VARIABLES',
}

export enum InternalPluginType {
  BASE = 'BASE',
  HOTKEYS = 'HOTKEYS',
  FAKE_SELECTION = 'FAKE_SELECTION',
  PREVENT_CALLBACKS = 'PREVENT_CALLBACKS',
}
