import { FlexCenter } from '@voiceflow/ui';

import { styled } from '@/hocs/styled';

const SliderPrefixContainer = styled(FlexCenter)`
  width: 24px;
  min-width: 24px;
  height: 24px;
  margin-right: 20px;
`;

export default SliderPrefixContainer;
