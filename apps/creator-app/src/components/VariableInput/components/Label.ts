import { styled } from '@/hocs/styled';

const Label = styled.span`
  color: #62778c;
  font-size: 13px;
  font-weight: 600;
  line-height: 1;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
`;

export default Label;
