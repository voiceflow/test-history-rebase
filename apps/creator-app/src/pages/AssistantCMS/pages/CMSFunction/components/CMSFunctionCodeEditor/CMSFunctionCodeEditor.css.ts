import { style } from '@voiceflow/style';

export const cmsFunctionButtonStyle = style({
  position: 'absolute',
  bottom: '0',
  width: '100%',
});

export const cmsFunctionCodeEditorStyle = style({
  height: 'calc(100% - 84px)',
});
