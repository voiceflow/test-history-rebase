import { styled } from '@/hocs/styled';

const SearchContainer = styled.div`
  position: fixed;
  top: 94px;
  left: 50%;
  z-index: 1050;
  width: 450px;
  margin-left: -225px;
`;

export default SearchContainer;
