import { Menu } from '@voiceflow/ui';

import { styled } from '@/hocs/styled';

const ReplySectionContainer = styled(Menu.Footer)`
  position: sticky;
  bottom: 0;
`;

export default ReplySectionContainer;
