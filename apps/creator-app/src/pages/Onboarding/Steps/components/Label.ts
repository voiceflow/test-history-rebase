import { styled } from '@/hocs/styled';

const SectionLabel = styled.div`
  color: #62778c;
  font-size: 15px;
  font-weight: 600;

  margin-bottom: 11px;
  margin-top: 24px;
  :first-child {
    margin-top: 0;
  }
`;

export default SectionLabel;
