import { styled } from '@/hocs/styled';

const Label = styled.div`
  text-transform: uppercase;
  color: #6e849a;
  font-size: 11px;
  font-weight: 600;
  font-stretch: normal;
`;

export default Label;
