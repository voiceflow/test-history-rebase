import { styled } from '@/hocs/styled';

const InlineContainer = styled.div`
  flex: 1;
  padding-left: 32px;
  overflow-x: hidden;
  overflow-x: clip;
  overflow-y: auto;
`;

export default InlineContainer;
