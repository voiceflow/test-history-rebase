export {
  /**
   * @deprecated Use `getErrorMessage` from `@/utils/error.util` instead
   */
  getErrorMessage,
  /**
   * @deprecated Use `normalizeError` from `@/utils/error.util` instead
   */
  normalizeError,
} from './error.test';
