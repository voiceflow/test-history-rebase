export const PAGE_SIZE = 50;

export enum Option {
  ACTIVE = 'Active',
  INACTIVE = 'Inactive',
}

export enum SessionType {
  GOOGLE = 'google',
  FACEBOOK = 'facebook',
  BASIC_AUTH = 'session',
  SIGN_UP = 'user',
}
