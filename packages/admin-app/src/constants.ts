export const PAGE_SIZE = 50;

export enum Option {
  ACTIVE = 'Active',
  INACTIVE = 'Inactive',
}

export enum SessionType {
  GOOGLE = 'google',
  BASIC_AUTH = 'session',
}

export enum ThemeType {
  DARK = 'DARK',
  LIGHT = 'LIGHT',
  UNICORN = 'UNICORN',
}
