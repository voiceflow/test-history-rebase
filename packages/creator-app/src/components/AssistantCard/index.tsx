import { AssistantCard as AssistantCardComponent, AssistantCardProps } from '@voiceflow/ui';
import React from 'react';

export const AssistantCard = (props: AssistantCardProps) => {
  return <AssistantCardComponent {...props} />;
};
