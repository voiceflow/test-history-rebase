import { Box } from '@voiceflow/ui';

import { styled } from '@/hocs/styled';

const LabelWrapper = styled(Box.Flex)`
  overflow-wrap: anywhere;
  display: inline-flex;
  flex-wrap: wrap;
`;

export default LabelWrapper;
