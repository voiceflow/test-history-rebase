import { styled } from '@/hocs';

const Body = styled.div`
  display: flex;
  width: 100%;
  height: 100%;
  flex-grow: 1;
  flex-direction: row;
  overflow: hidden;
  overflow: clip;
  position: relative;
`;

export default Body;
