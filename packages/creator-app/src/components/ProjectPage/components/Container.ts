import { styled } from '@/hocs';

const Container = styled.section`
  position: relative;
  display: flex;
  width: 100vw;
  height: 100%;
  flex-direction: column;
  overflow: hidden;
  overflow: clip;
`;

export default Container;
