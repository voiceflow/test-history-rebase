import { styled } from '@/hocs';

const Container = styled.section`
  position: relative;
  display: flex;
  height: 100vh;
  width: 100vw;
  flex-direction: column;
  overflow: hidden;
  overflow: clip;
`;

export default Container;
