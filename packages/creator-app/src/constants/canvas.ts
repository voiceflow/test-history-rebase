export enum BlockState {
  REGULAR = 'regular',
  SELECTED = 'selected',
  ACTIVE = 'active',
  HOVERED = 'hovered',
  DISABLED = 'disabled',
}

export enum StepLabelVariant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  PLACEHOLDER = 'placeholder',
}
