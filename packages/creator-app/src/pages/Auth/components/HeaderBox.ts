import { styled } from '@/hocs/styled';

const HeaderBox = styled.div`
  text-align: left;
  margin-bottom: 24px;

  h1 {
    font-size: 15px;
    font-weight: 600;
    margin-bottom: 0;
  }
`;

export default HeaderBox;
