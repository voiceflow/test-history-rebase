import { flexApartStyles } from '@/components/Flex';
import { styled } from '@/hocs';

const TitleActionsContainer = styled.div.attrs({ fullWidth: true })`
  ${flexApartStyles}
`;

export default TitleActionsContainer;
