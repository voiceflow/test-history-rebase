import { units } from '@voiceflow/ui';

import { styled } from '@/hocs';

const HeaderTitle = styled.h3`
  font-size: 18px;
  font-weight: 700;
  margin: 0 ${units(1.25)}px 0 0;
`;

export default HeaderTitle;
