import { Box } from '@voiceflow/ui';

import { styled } from '@/hocs/styled';

export const BuiltInIntentMessage = styled.div`
  font-size: 13px;
  color: #62778c;
  padding-bottom: 20px;
`;

export const MessageWrapper = styled(Box)`
  position: relative;
  padding: 0 32px 1px;
  margin-top: -10px;
  background: #fdfdfd;
`;
