import { Flex } from '@/components/Box';
import { styled } from '@/hocs';

const SettingsColorsContainer = styled(Flex)`
  width: 232px;
  padding: 8px 16px;
  flex-wrap: wrap;
`;

export default SettingsColorsContainer;
