import { FlexApart } from '@/components/Flex';
import { styled } from '@/hocs';

const ShortcutContainer = styled(FlexApart)`
  padding: 10px 0;
  color: #132144;
  font-size: 13px;
  font-weight: 600;
`;

export default ShortcutContainer;
