import { FlexCenter } from '@voiceflow/ui';

import { styled } from '@/hocs';

const CenterContainer = styled(FlexCenter)`
  z-index: 20;
  position: relative;
  height: 0px;
`;

export default CenterContainer;
