import { FlexCenter } from '@voiceflow/ui';

import { styled } from '@/hocs';

const UploadContainer = styled(FlexCenter)`
  flex-direction: row;
  flex: 1;
  align-self: flex-start;
`;

export default UploadContainer;
