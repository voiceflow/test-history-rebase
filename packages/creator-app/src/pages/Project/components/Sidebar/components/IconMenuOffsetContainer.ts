import { styled } from '@/hocs';

export const IconMenuOffsetContainer = styled.div`
  padding-left: ${({ theme }) => theme.components.sidebarIconMenu.width}px;
  height: 100%;
`;

export default IconMenuOffsetContainer;
