import { styled } from '@/hocs';

const TopLevelOuterContainer = styled.aside`
  position: absolute;
  top: 16px;
  left: 331px;
  z-index: 10;
  border-radius: 8px;
  background-color: rgba(241, 241, 241, 0.65);
  padding: 4px 4px 0;
  z-index: 10;
`;

export default TopLevelOuterContainer;
