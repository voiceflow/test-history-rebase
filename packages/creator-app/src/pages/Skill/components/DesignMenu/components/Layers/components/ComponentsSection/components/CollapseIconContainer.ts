import { FlexCenter } from '@voiceflow/ui';

import { styled } from '@/hocs';

const CollapseIconContainer = styled(FlexCenter)`
  width: 16px;
  height: 16px;
  cursor: pointer;
  transform: rotate(-90deg);
`;

export default CollapseIconContainer;
