import { FlexCenter } from '@voiceflow/ui';

import { styled } from '@/hocs';

const TopicNameIconContainer = styled(FlexCenter)`
  width: 22px;
  height: 22px;
  margin-right: 3px;
`;

export default TopicNameIconContainer;
