const SIDEBAR_WIDTH = 72;
const HEADER_HEIGHT = 72;

const PROJECT_PAGE = {
  header: {
    height: HEADER_HEIGHT,
  },

  sidebar: {
    width: SIDEBAR_WIDTH,
  },
} as const;

export default PROJECT_PAGE;
