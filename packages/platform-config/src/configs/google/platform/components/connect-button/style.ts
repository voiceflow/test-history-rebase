import { BaseButton, styled } from '@voiceflow/ui';

export const Button = styled(BaseButton)`
  position: relative;
  height: 40px;
  color: #fff;
  background: #4285f4;
  border-radius: 2px;

  span:last-of-type {
    padding: 0 22px 0 18px;
  }
`;
