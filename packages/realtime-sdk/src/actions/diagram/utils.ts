import { DIAGRAM_KEY } from '@realtime-sdk/constants';
import { Utils } from '@voiceflow/common';

// eslint-disable-next-line import/prefer-default-export
export const diagramType = Utils.protocol.typeFactory(DIAGRAM_KEY);
