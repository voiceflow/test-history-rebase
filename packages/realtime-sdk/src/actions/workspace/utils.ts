import { WORKSPACE_KEY } from '../../constants';
import { typeFactory } from '../utils';

// eslint-disable-next-line import/prefer-default-export
export const workspaceType = typeFactory(WORKSPACE_KEY);
