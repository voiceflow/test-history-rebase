{
  "name": "@voiceflow/realtime",
  "version": "1.2.0",
  "dependencies": {
    "@logux/server": "^0.10.0",
    "@voiceflow/common": "^6.5.1",
    "@voiceflow/logger": "^1.5.6",
    "@voiceflow/realtime-sdk": "^1.2.0",
    "core-js": "^3.12.1",
    "regenerator-runtime": "^0.13.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "yarn clean && ttsc --project tsconfig.build.json",
    "clean": "rimraf build",
    "e2e": "yarn kill:e2e && NODE_ENV=e2e nodemon src/index.ts",
    "e2e:node-dev": "yarn kill:e2e && NODE_ENV=e2e node-dev -r esm  src/index.ts",
    "kill:e2e": "kill -9 $(lsof -t -i tcp:8013) || true",
    "lint": "eslint \"**/*.{js,ts}\"",
    "lint:fix": "yarn lint --fix",
    "lint:quiet": "yarn lint --quiet",
    "lint:report": "yarn lint --format json -o sonar/report.json",
    "start": "NODE_ENV=production node --experimental-specifier-resolution=node --max-old-space-size=8192 build/index.js",
    "test": "exit 0",
    "test:dependencies": "depcheck",
    "test:integration": "exit 0",
    "test:unit": "exit 0"
  },
  "type": "module"
}
