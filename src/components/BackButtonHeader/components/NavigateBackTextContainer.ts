import { styled } from '@/hocs';

const NavigateBackTextContainer = styled.div`
  color: ${({ theme }) => theme.colors.primary};
  padding-left: 10px;
  padding-right: 10px;
  font-weight: 500;
  font-size: 15px;
`;

export default NavigateBackTextContainer;
