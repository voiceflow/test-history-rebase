import { styled } from '@/hocs';

const CheckBoxContainer = styled.label`
  position: relative;
  display: flex;
  align-items: center;
  min-width: 0;
  margin-bottom: 0;
  text-align: left;
  cursor: pointer;
`;

export default CheckBoxContainer;
