import { styled, units } from '@/hocs';

const ExpandingListSectionContainer = styled.div`
  padding: ${units()}px 0;
  padding-top: 0;
  margin-bottom: 0 !important;
`;

export default ExpandingListSectionContainer;
