import Badge from '@/componentsV2/Badge';
import { styled } from '@/hocs';

const AddUtteranceRightAction = styled(Badge)`
  animation: fadein 0.15s ease, moveinleft 0.15s ease;
`;

export default AddUtteranceRightAction;
