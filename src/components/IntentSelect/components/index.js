import AlertMessage from '@/components/AlertMessage';
import { styled } from '@/hocs';

export { default as Option } from './IntentOption';

export const MissingIntentMessage = styled(AlertMessage)`
  margin-top: 10px;
`;
