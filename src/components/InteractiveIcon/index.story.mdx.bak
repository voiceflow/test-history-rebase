import { Meta, Preview, Story } from '@/../.storybook';

import * as stories from './index.story';

import InteractiveIcon from '.';

<Meta title="Interactive Icon" component={InteractiveIcon} />

# Interactive Icon

A icon button that can have various information properties attached to it

<Preview>
  <Story name="plus">{stories.plus()}</Story>
  <Story name="minus">{stories.minus()}</Story>
</Preview>
