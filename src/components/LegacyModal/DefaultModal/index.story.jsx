import React from 'react';

import DefaultModal from '.';

export default {
  title: 'Modal/Default',
  component: DefaultModal,
  includeStories: [],
};

export const normal = () => <DefaultModal open />;
