import { styled } from '@/hocs';

const ToggleSwitchContainer = styled.div`
  position: relative;
  height: 20px;
  margin-top: -2px;
  margin-right: -4px;
  margin-left: -4px;
  transform: scale(0.8);
`;

export default ToggleSwitchContainer;
