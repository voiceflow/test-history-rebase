import { SvgIconContainer } from '@/components/SvgIcon';
import { styled } from '@/hocs';

const SlotMappingArrowContainer = styled(SvgIconContainer)`
  margin: 8px;
`;

export default SlotMappingArrowContainer;
