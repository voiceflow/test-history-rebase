import styled from 'styled-components';

const CaptionedIconButtonLabel = styled.span`
  width: inherit;
  padding: 0 8px;
  overflow-x: hidden;
  box-sizing: border-box;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-weight: 600;
  font-size: 11px;
  line-height: 15px;
`;

export default CaptionedIconButtonLabel;
