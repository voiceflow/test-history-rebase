import { Meta, Preview, Story } from '@/../.storybook';

import * as stories from './index.story';

import CaptionedIconButton from '.';

<Meta title="Captioned Icon Button" component={CaptionedIconButton} />

# Captioned Icon Button

A basic icon button with a label.

<Preview>
  <Story name="normal">{stories.normal()}</Story>
</Preview>
