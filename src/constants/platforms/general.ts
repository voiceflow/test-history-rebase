export enum GeneralStageType {
  IDLE = 'IDLE',
  ERROR = 'ERROR',
  SUCCESS = 'SUCCESS',
  PROGRESS = 'PROGRESS',
}

export enum GeneralJobErrorType {
  RENDERING = 'RENDERING',
}

export enum GeneralJobSuccessType {
  SUCCESS = 'SUCCESS',
  DOWNLOAD = 'DOWNLOAD',
}
