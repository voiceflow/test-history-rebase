import { AvInput } from 'availity-reactstrap-validation';

import { styled } from '@/hocs';

const Input = styled(AvInput)`
  width: 90%;
  min-height: 43px;
  padding-left: 10px 16px 12px 16px;
`;

export default Input;
