import styled from 'styled-components';

import { FlexAround } from '@/componentsV2/Flex';

const CanvasWarningContainer = styled(FlexAround)`
  width: 400px;
  border-radius: 5px;
  padding: 15px 20px;
  background-color: white;
  box-shadow: 0 0 1px 1px rgba(17, 49, 96, 0.06), 0 2px 4px 0 rgba(17, 49, 96, 0.08);
`;

export default CanvasWarningContainer;
