import { Meta, Preview, Story } from '@/../.storybook';

import * as stories from './index.story';

import DeleteWorkspaceModal from '.';

<Meta title="Dashboard/Delete Workspace Modal" component={DeleteWorkspaceModal} />

# Delete Workspace Modal

A modal to check when the user deletes a workspace.

<Preview>
  <Story name="normal">{stories.normal()}</Story>
</Preview>
