import { styled, units } from '@/hocs';

const BlockHeader = styled.div`
  padding: ${units(0.5)}px 0 ${units(1.5)}px 0;
  color: #62778c;
  font-weight: 600;
  font-size: 1.1em;
`;

export default BlockHeader;
