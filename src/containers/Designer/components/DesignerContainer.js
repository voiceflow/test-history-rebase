import { FlexCenter } from '@/componentsV2/Flex';
import { styled } from '@/hocs';

const DesignerContainer = styled(FlexCenter)`
  height: 100%;
  font-size: 14px;
  line-height: 18px;
  color: #132144;
`;

export default DesignerContainer;
