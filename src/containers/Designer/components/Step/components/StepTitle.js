import { styled, units } from '@/hocs';

const StepTitle = styled.span`
  margin-right: ${units(0.5)}px;
  text-transform: uppercase;
  font-weight: 600;
  color: #0f7ec0;
`;

export default StepTitle;
