import Checkbox from '@/components/Checkbox';
import { styled } from '@/hocs';

const LocaleCheckbox = styled(Checkbox)`
  padding: 10px;
  align-items: center;
  background-position: 15px center;
  margin: 5px 10px;
  display: flex;
`;

export default LocaleCheckbox;
