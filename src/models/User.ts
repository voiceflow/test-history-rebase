export type LockOwnerType = {
  name: string | null;
  email: string | null;
  role: string | null;
  image: string | null;
  // eslint-disable-next-line camelcase
  creator_id: number | null;
  seats: number | null;
  created: string | null;
  color: string | null;
};
