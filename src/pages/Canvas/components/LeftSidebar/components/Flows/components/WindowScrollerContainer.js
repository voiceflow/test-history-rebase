import { styled } from '@/hocs';

const WindowScrollerContainer = styled.div`
  position: relative;
  width: 100%;
  padding: 13px 0;
  overflow-x: hidden;
`;

export default WindowScrollerContainer;
