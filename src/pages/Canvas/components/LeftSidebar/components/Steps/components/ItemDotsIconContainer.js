import Flex from '@/components/Flex';
import { styled } from '@/hocs';

const ItemDotsIconContainer = styled(Flex)`
  position: absolute;
  top: 0px;
  right: 12px;
  bottom: 0px;
`;

export default ItemDotsIconContainer;
