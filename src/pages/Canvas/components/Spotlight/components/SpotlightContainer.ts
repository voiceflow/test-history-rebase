import { styled } from '@/hocs';

const SpotlightContainer = styled.div`
  position: fixed;
  top: 150px;
  left: 50%;
  z-index: 1050;
  width: 36%;
  margin-left: -18%;
  padding-left: 20px;
`;

export default SpotlightContainer;
