export enum ActivationMode {
  FOCUS = 'focus',
  SELECTION = 'selection',
}

export enum EntityType {
  NODE = 'node',
  PORT = 'port',
  LINK = 'link',
  THREAD = 'thread',
  PORT_LINK = 'port-link',
}
