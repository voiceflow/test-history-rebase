import { Meta, Preview, Story } from '@/../.storybook';

import { withDnD } from '@/../.storybook';

import * as stories from './index.story';

import MarkupShapeEditor from '.';

<Meta title="Creator/Editors/Markup Shape" component={MarkupShapeEditor} decorators={[withDnD]} />

# Markup Shape Editor

The editor for "markup shape" blocks.

<Preview>
  <Story name="Circle">{stories.circle()}</Story>
  <Story name="Rectangle">{stories.rectangle()}</Story>
  <Story name="Line">{stories.line()}</Story>
  <Story name="Arrow">{stories.arrow()}</Story>
</Preview>
