import { Meta, Preview, Story } from '@/../.storybook';

import * as stories from './index.story';
import ReminderStep from '.';

<Meta title="Creator/Steps/Reminder Step" component={ReminderStep} />

# Reminder Step

<Preview>
  <Story name="empty">{stories.empty()}</Story>
  <Story name="with long label">{stories.withLonglabel()}</Story>
  <Story name="without ports">{stories.withoutPorts()}</Story>
  <Story name="active">{stories.active()}</Story>
  <Story name="connected">{stories.connected()}</Story>
</Preview>
