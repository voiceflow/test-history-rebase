import { FlexCenter } from '@/components/Flex';
import { styled } from '@/hocs';

const CollaboratorsCount = styled(FlexCenter as any)`
  margin-right: 13px;
  font-size: 15px;
  letter-spacing: 0.36px;
`;

export default CollaboratorsCount;
