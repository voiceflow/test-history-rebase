import { Meta, Preview, Story } from '_storybook';
import * as stories from './index.story';
import Onboarding from '.';

<Meta title="Creator/Onboarding" component={Onboarding} />

# Onboarding Modal

<Preview>
  <Story name="create">{stories.createWorkspace()}</Story>
  <Story name="join">{stories.joinWorkspace()}</Story>
</Preview>
