import React from 'react';

import { withOnboarding } from '../context';

const CreateWorkspace: React.FC = () => {
  return <div>Create Workspace Step Content...</div>;
};

export default withOnboarding(CreateWorkspace);
