import { styled } from '@/hocs';

const SectionLabel = styled.div`
  color: #62778c;
  font-size: 15px;
  font-weight: 600;

  margin-bottom: 14px;
  margin-top: 24px;
  :first-child {
    margin-top: 0;
  }
`;

export default SectionLabel;
