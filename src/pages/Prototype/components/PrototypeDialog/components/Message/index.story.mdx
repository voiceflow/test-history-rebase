import { Meta, Preview, Story } from '@/../.storybook';

import story, * as stories from './index.story';

import Message from '.';

<Meta title={story.title} component={story.component} />

# Chat

Chat Bubble Variantions for the Dialog in Prototyping

## Variants

<Preview>
  <Story name="standard">{stories.standard()}</Story>
  <Story name="audio">{stories.audio()}</Story>
  <Story name="speak">{stories.speak()}</Story>
  <Story name="debug">{stories.debug()}</Story>
  <Story name="input">{stories.user()}</Story>
  <Story name="loading">{stories.loading()}</Story>
</Preview>
