import { Meta, Preview, Story } from '_storybook';

import story, * as stories from './index.story';

import Start from '.';

<Meta title={story.title} component={story.component} />

# Start

<Preview>
  <Story name="base">{stories.base()}</Story>
</Preview>
