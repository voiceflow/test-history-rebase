import { MenuContainer as BaseMenuContainer } from '@/components/Menu';
import { styled } from '@/hocs';

const MenuContainer = styled(BaseMenuContainer)`
  margin-top: 10px;
  padding: 18px 24px;
  white-space: normal;
`;
export default MenuContainer;
