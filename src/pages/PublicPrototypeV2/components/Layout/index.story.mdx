import { Meta, Preview, Story } from '@/../.storybook';

import story, * as stories from './index.story';

import Layout from '.';

<Meta title={story.title} component={story.component} />

# Layout

## Variants

<Preview>
  <Story name="standard">{stories.basic()}</Story>
  <Story name="splash screen passed">{stories.splashScreenPassed()}</Story>
  <Story name="visuals">{stories.visuals()}</Story>
  <Story name="visuals splash screen passed">{stories.visualsSplashScreenPassed()}</Story>
</Preview>
