import { Meta, Preview, Story } from '_storybook';

import story, * as stories from './index.story';

<Meta title={story.title} component={story.component} />

# Share Splash Screens

<Preview>
  <Story name="withDialog">{stories.withDialog()}</Story>
  <Story name="mobile">{stories.mobile()}</Story>
  <Story name="mobileInstruction">{stories.mobileInstruction()}</Story>
</Preview>
