import { FlexCenter } from '@/components/Flex';
import { styled } from '@/hocs';

export const HeaderText = styled(FlexCenter)`
  width: 100%;
  text-align: center;
  font-size: 18px;
  font-weight: 500;
  color: #132144;
`;
export const TextContainer = styled.span`
  position: relative;
  right: 54px;
`;
